import { FilterByInputTextPipe } from './filter-by-input-text.pipe';

describe('FilterByInputTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByInputTextPipe();
    expect(pipe).toBeTruthy();
  });
});
